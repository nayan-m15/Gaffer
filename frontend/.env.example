# vite.config.ts currently loads Vite variables from the repository-root .env.
# Optional local-development backend URL. Production always uses the
# same-origin /auth and /api Vercel rewrites so auth cookies remain first-party.
VITE_API_URL=http://localhost:3000
